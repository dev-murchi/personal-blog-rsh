<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Blog Articles</title>
  <link rel="stylesheet" href="/css/article-form.css">
</head>

<body>
  <div class="container">
    <h1>
      <%= title %>
    </h1>
    <form id="editForm" method="post">
      <div class="form-group">
        <label for="title">Article Title</label>
        <input type="text" id="title" name="title" value="<%= article.title === undefined ? '' : article.title %>"
          required>
      </div>
      <div class="form-group">
        <label for="publishDate">Creation Date</label>
        <input type="date" id="publishDate" name="publishDate"
          value="<%= article.publishDate === undefined ? '' : article.publishDate %>" required>
      </div>
      <div class="form-group">
        <label for="content">Article Body</label>
        <textarea rows="20" id="content" name="content"
          required><%= article.content === undefined ? '' : article.content %></textarea>
      </div>
      <div class="form-group">
        <button type="submit">Save Changes</button>
      </div>
    </form>
    <script>
    </script>
</body>

</html>